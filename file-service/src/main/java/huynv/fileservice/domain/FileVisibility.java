package huynv.fileservice.domain;

/**
 * Defines the read-visibility modes supported by file-service authorization rules.
 */
public enum FileVisibility {
    PRIVATE,
    TENANT_SHARED,
    PUBLIC
}

