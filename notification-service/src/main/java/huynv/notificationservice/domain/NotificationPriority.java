package huynv.notificationservice.domain;

/**
 * Defines scheduling priority for notifications used by internal priority queues.
 */
public enum NotificationPriority {
    HIGH,
    NORMAL,
    LOW
}

