{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "user.created.v1",
  "title": "user.created.v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["eventId", "eventType", "source", "eventTime", "aggregateId", "aggregateVersion", "dataSchema", "data"],
  "properties": {
    "eventType": { "const": "user.created.v1" },
    "dataSchema": { "const": "user.created.v1" },
    "data": {
      "type": "object",
      "additionalProperties": true,
      "required": ["userId", "keycloakUserId", "tenantId", "status", "createdAt"],
      "properties": {
        "userId": { "type": "string", "format": "uuid" },
        "keycloakUserId": { "type": "string", "format": "uuid" },
        "tenantId": { "type": "string", "format": "uuid" },
        "email": { "type": ["string", "null"] },
        "fullName": { "type": ["string", "null"] },
        "phoneNumber": { "type": ["string", "null"] },
        "avatarUrl": { "type": ["string", "null"] },
        "status": { "type": "string", "minLength": 1 },
        "locale": { "type": ["string", "null"] },
        "timezone": { "type": ["string", "null"] },
        "createdAt": { "type": "string", "minLength": 1 }
      }
    }
  }
}

