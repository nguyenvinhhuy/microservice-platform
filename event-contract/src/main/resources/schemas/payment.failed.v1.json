{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "payment.failed.v1",
  "title": "payment.failed.v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["eventId", "eventType", "source", "eventTime", "aggregateId", "aggregateVersion", "dataSchema", "data"],
  "properties": {
    "eventType": { "const": "payment.failed" },
    "dataSchema": { "const": "payment.failed.v1" },
    "data": {
      "type": "object",
      "additionalProperties": true,
      "required": ["orderId", "paymentId", "tenantId", "reason"],
      "properties": {
        "orderId": { "type": "string", "minLength": 1 },
        "paymentId": { "type": "string", "minLength": 1 },
        "tenantId": { "type": "integer" },
        "reason": { "type": "string", "minLength": 1 }
      }
    }
  }
}
