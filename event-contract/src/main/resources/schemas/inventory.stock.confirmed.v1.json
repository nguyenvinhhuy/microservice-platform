{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "inventory.stock.confirmed.v1",
  "title": "inventory.stock.confirmed.v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["eventId", "eventType", "source", "eventTime", "aggregateId", "aggregateVersion", "dataSchema", "data"],
  "properties": {
    "eventType": { "const": "inventory.stock.confirmed" },
    "dataSchema": { "const": "inventory.stock.confirmed.v1" },
    "data": { "type": "object", "additionalProperties": true }
  }
}
