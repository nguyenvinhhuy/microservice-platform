<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.platform</groupId>
    <artifactId>microservice-platform</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>microservice-platform</name>
    <description>Build aggregator for shared libraries and core e-commerce services.</description>

    <properties>
        <spotless.maven.plugin.version>2.46.1</spotless.maven.plugin.version>
        <google.java.format.version>1.24.0</google.java.format.version>
    </properties>

    <modules>
        <module>event-contract</module>
        <module>event-infra</module>
        <module>order-service</module>
        <module>product-service</module>
        <module>inventory-service</module>
        <module>payment-service</module>
        <module>notification-service</module>
        <module>product-view-service</module>
        <module>order-view-service</module>
        <module>dlq-replayer-service</module>
        <module>gateway-service</module>
        <module>audit-log-service</module>
    </modules>

    <build>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.maven.plugin.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>**/src/*/java/**/*.java</include>
                        </includes>
                        <googleJavaFormat>
                            <version>${google.java.format.version}</version>
                        </googleJavaFormat>
                        <removeUnusedImports/>
                        <formatAnnotations/>
                    </java>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>


