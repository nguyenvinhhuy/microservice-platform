---
# Audit Log Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-log-service
  namespace: microservice-platform
  labels:
    app: audit-log-service
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: audit-log-service
  template:
    metadata:
      labels:
        app: audit-log-service
        version: v1
    spec:
      containers:
      - name: audit-log-service
        image: your-registry/audit-log-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8005
          name: http
        env:
        - name: SERVER_PORT
          value: "8005"
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres-db:5432/audit_db
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: AUDIT_DB_USER
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: AUDIT_DB_PASSWORD
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: KAFKA_BOOTSTRAP_SERVERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8005
          initialDelaySeconds: 40
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8005
          initialDelaySeconds: 20
          periodSeconds: 5

---
# Audit Log Service
apiVersion: v1
kind: Service
metadata:
  name: audit-log-service
  namespace: microservice-platform
  labels:
    app: audit-log-service
spec:
  selector:
    app: audit-log-service
  ports:
  - protocol: TCP
    port: 8005
    targetPort: 8005
    name: http
  type: ClusterIP

---
# Audit Log Service HPA
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: audit-log-service-hpa
  namespace: microservice-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: audit-log-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

